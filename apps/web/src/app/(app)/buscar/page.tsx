import { BuscarView } from '@/features/leads/components/buscar-view';

export const metadata = { title: 'Buscar Leads — ConvertAI' };

export default function BuscarPage() {
  return <BuscarView />;
}
