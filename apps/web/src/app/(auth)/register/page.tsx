import { RegisterForm } from '@/features/auth/components/register-form';

export const metadata = { title: 'Criar conta — ConvertAI' };

export default function RegisterPage() {
  return <RegisterForm />;
}
