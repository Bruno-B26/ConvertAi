import { ForgotPasswordForm } from '@/features/auth/components/forgot-password-form';

export const metadata = { title: 'Recuperar senha — ConvertAI' };

export default function ForgotPasswordPage() {
  return <ForgotPasswordForm />;
}
